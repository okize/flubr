@import 'variables'

.nav-drop-shadow
  box-shadow: -10px 0 15px rgba($navShadowColor, .25)

.nav-inset-shadow
  box-shadow: inset -5px 0 15px rgba($navShadowColor, .15)

.app-navigation
  @extend .nav-inset-shadow
  background-color: $navBgColor

.navigation-logo
  @extend .nav-inset-shadow
  background-color: $flubr
  color: $flubrText
  height: 10rem
  text-align: center
  text-transform: uppercase
  padding-top: 4rem
  font-size: 2.5rem

.navigation-user
  @extend .nav-inset-shadow
  background-color: $navUserBgColor
  color: $navUserColor
  padding: 1rem
  .avatar
    margin-right: 1rem

.navigation-links
  // display: none
  margin: 0
  padding: 0
  list-style: none
  a, &:visited
    color: $navLinkColor
    padding: 1rem
    display: block
    text-decoration: none
    text-transform: uppercase
    font-size: 1rem
    border-bottom: .1rem solid $navLinkBorderColor
    &:hover
      color: $navLinkHoverColor
      background-color: $navLinkHoverBgColor
  .active
    a
      @extend .nav-drop-shadow
    a, a:hover
      background-color: $navLinkActiveBgColor
      color: $navLinkActiveColor
      cursor: default
  // +from($medium-up)
  //   display: block