@import 'variables'

.nav-drop-shadow
  box-shadow: 5px 5px 15px rgba($navShadowColor, .15)

.nav-inset-shadow-left
  box-shadow: inset -5px -5px 15px rgba($navShadowColor, .15)

.nav-inset-shadow-right
  box-shadow: inset 5px -5px 15px rgba($navShadowColor, .15)