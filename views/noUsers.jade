extends layout

block content

  .error-page
    h1 No users found!
    p Enter your Twitter handle below to add yourself as the first user.
    p After you log into the application you can authorize more users.

    form(method='post', action='/addFirstUser')
      input(name='firstUser', value='true', type='hidden')
      input.inputAddOn-field(name='user', type='text', placeholder='Twitter handle')
      p
        button(type='submit') Add user
